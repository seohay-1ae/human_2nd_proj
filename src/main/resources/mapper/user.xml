<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.travelquest.user.dao.UserDAO">
    <resultMap id="usersResult" type="com.project.travelquest.user.vo.UserVO">
        <result property="user_id" column="user_id" />
        <result property="user_pw" column="user_pw" />
        <result property="user_name" column="user_name" />
        <result property="user_email" column="user_email" />
        <result property="user_phoneNumber" column="user_phoneNumber" />
        <result property="joinDate" column="joinDate" />
    </resultMap>

    <select id="selectAllUsersList" resultType="usersList">
        <![CDATA[
			select * from tq_user
			 where user_id=#{user_id}
			 and user_pw=#{user_pw}

		]]>
    </select>

<!--    <select id="selectOverlappedID" parameterType="String" resultType="String">-->
<!--        select decode(count(*),1, 'true', 0, 'false')-->
<!--        from tq_user-->
<!--        where user_id = #{id}-->
<!--    </select>-->

<!--    <insert id="insertNewUser"  parameterType="userVO">-->
<!--        <![CDATA[-->
<!--    	insert into tq_user(user_id, user_pw, user_name, user_phoneNumber)-->
<!--			values(#{user_id},-->
<!--			       #{user_pw},-->
<!--			       #{user_name},-->
<!--			       #{user_phoneNumber}-->
<!--			       )-->


<!--   ]]>-->
<!--    </insert>-->
</mapper>