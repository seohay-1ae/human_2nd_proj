<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.travelquest.user.dao.UserDAO">
    <select id="existsByEmail" resultType="int">
        SELECT COUNT(*) FROM t_users WHERE email = #{email}
    </select>


    <insert id="saveUser" parameterType="com.project.travelquest.user.vo.UserVO">
        INSERT INTO t_users (
        email, password, username, phone_number, cert_number
        ) VALUES (
        #{email}, #{password}, #{username}, #{phone_number}, #{cert_number}
        )
    </insert>

</mapper>