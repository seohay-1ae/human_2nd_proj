<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.travelquest.user.dao.UserDAO">

<!--    &lt;!&ndash; 이메일 중복 검사 &ndash;&gt;-->
<!--    <select id="existsByEmail" resultType="int" parameterType="String">-->
<!--        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END-->
<!--        FROM t_users-->
<!--        WHERE email = #{email}-->
<!--    </select>-->

    <!-- 회원정보 저장 -->
<!--    <insert id="saveUser" parameterType="UserVO">-->
<!--        INSERT INTO t_users (-->
<!--        email, password, password_check, username, phone_number, cert_number-->
<!--        ) VALUES (-->
<!--        #{email}, #{password}, #{password_check}, #{username}, #{phone_number}, #{cert_number}-->
<!--        )-->
<!--    </insert>-->

</mapper>